#include "global.h"
#include "userguide.h"
void userguide(){
  printf("Wrong format!\n" );
  printf("Please specify the Game-ID and the player number.\n" );
  printf("The program needs to be run like this in order for it to function properly:\n" );
  printf("./sysprak-client -g <GAME-ID> -p <{1,2}>\n" );
}

